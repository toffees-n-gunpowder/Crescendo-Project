# Intentionally empty. This project uses raw SQL, not the Django ORM.
# Data access lives in music/db/ and music/auth/.
# The schema is defined by the scripts in docs/schema/ and applied with:
#     python manage.py apply_schema
